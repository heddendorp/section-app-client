query getTenantCurrencyCode {
  currentTenant {
    id
    currency
  }
}

query getAppStartupInfo {
  currentTenant {
    id
    currency
    settings {
      banners {
        title
        body
        color
        link
        displayToMembershipStatus
      }
    }
  }
}
