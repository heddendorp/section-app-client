import { IfStatusDirective } from './if-status.directive';

describe('IfStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new IfStatusDirective();
    expect(directive).toBeTruthy();
  });
});
