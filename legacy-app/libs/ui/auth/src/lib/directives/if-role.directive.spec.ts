import { IfRoleDirective } from './if-role.directive';

describe('IfRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new IfRoleDirective();
    expect(directive).toBeTruthy();
  });
});
