import { ResetScrollDirective } from './reset-scroll.directive';

describe('ResetScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ResetScrollDirective();
    expect(directive).toBeTruthy();
  });
});
